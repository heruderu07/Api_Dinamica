generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}


model USD_BRL {
  id          Int      @id @default(autoincrement())
  create_date DateTime @unique
  high        Float
  low         Float
  volume      Float
  createdAt   DateTime @default(now())
}